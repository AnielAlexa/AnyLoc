# AnyLoc Raspberry Pi Requirements (CPU-only, minimal)
# Heavily optimized for ARM64 CPU performance

# Core ML (CPU-only versions)
numpy>=1.21.0,<1.24.0  # Pin for ARM compatibility
scipy>=1.7.0,<1.10.0

# Computer vision (minimal)
opencv-python>=4.5.0
Pillow>=8.3.0

# Essential tensor operations only
einops>=0.4.0

# Lightweight clustering (CPU-optimized)
scikit-learn>=1.0.0,<1.3.0

# Configuration
PyYAML>=6.0
tqdm>=4.62.0

# Minimal monitoring
psutil>=5.8.0

# Data handling (lightweight)
h5py>=3.3.0

# Remove heavy dependencies:
# - No CUDA libraries
# - No advanced visualization
# - No development tools
# - No web frameworks (too heavy for RPi)

# Optional: Ultra-lightweight monitoring
flask>=2.0.0,<2.3.0  # If web interface needed